<li id="{{_id}}">
    <img width="100" height="100" src="/static/images/{{#if profilePic}}{{profilePic}}{{else}}profile.png{{/if}}">
    <h2>{{username}}</h2>
    <p>{{friendsCount}} friends</p>
    {{#if isFriend}}
    <p>friends</p>
    {{else}}
    {{#if isRequested}}
    <a href="javascript:void(0)">Accept</a>
    {{else}}
    <a href="javascript:void(0)">Add</a>
    {{/if}}
    {{/if}}
</li>